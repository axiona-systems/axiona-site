# AXIONA R35 A-only Icon Integration

Fix:
- The header no longer uses a large source image squeezed into the small box.
- The header uses only the A-mark asset from the logo pack:
  `assets/brand/axiona-a-mark-r35.svg`
- The small header box contains only the A symbol.
- The text `AXIONA Systems` remains live HTML text.

Icon/PWA:
- r35 cache-busted favicon, Apple touch and manifest icons are active.
