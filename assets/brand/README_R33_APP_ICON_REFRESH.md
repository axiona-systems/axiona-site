# AXIONA R33 App Icon Refresh + Header Mark Fix

Header:
- Inline SVG mark widened and strengthened.
- Text remains live HTML: `AXIONA Systems`.

App / browser icon:
- r33 filenames added for favicon, Apple touch icon, manifest icons, maskable icons, social image.
- Manifest includes an explicit `id` and `start_url` version.

Important:
- Chrome/Safari may continue showing the old icon in the browser "Open in app" chip if the site was already installed.
- Remove the old installed web app / home-screen entry, close the browser, reopen the site, then add/install it again.
- Google search favicon is crawler/cache controlled and can take days or longer to update.
