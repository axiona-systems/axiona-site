# R115 Keeper visual story calibration

R115 removes the relationship-map visual grammar from the overview hero and replaces it with a Keeper-derived working surface. The System Story now uses one shared petrol frame with stable off-white steps; active scroll state no longer flips full card colors. Reveal motion is repeatable when elements leave and re-enter the viewport. Hero pointer tilt is removed.
