# R114 cache-proof visual binding

The overview now binds R110, R112, R113 and R113 tuning directly from HU/EN/DE HTML with `release=R114` query strings. This removes reliance on a previously cached R110 parent stylesheet import chain.
